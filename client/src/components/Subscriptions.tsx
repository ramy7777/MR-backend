import React from 'react';
import { Box, Typography } from '@mui/material';

const Subscriptions = () => {
  return (
    <Box>
      <Typography variant="h4">Subscriptions Management</Typography>
    </Box>
  );
};

export default Subscriptions;
